$( "#inputfield" ).keyup(function() { $("#inputfield").val( $(".highlight").text() )});
//Dán vào console, ô input tự động điền và bạn chỉ cần nhấn Space
//Paste it into the console, the input box fills in automatically, and you just need to press Space
